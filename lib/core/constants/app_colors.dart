/// core/constants/app_colors.dart
/// Defines the OLED-ready color palette for the e-commerce experience.

import 'package:flutter/material.dart';

/// Centralized color tokens to keep the UI consistent and WCAG compliant.
class AppColors {
  AppColors._();

  static const Color background = Color(0xFF000000);
  static const Color surface = Color(0xFF121212);
  static const Color primary = Color(0xFF00D4AA);
  static const Color primaryDark = Color(0xFF00A486);
  static const Color secondary = Color(0xFFFF4B4B);
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFB0B0B0);
  static const Color divider = Color(0xFF1F1F1F);
  static const Color success = Color(0xFF4ADE80);
  static const Color warning = Color(0xFFFFB74D);
}

